// Buscar todas las instancias de "ProAr"
/*const elementos = document.getElementById("nosotros");
for (let i = 0; i < elementos.length; i++) {
  const elemento = elementos[i];
  // Buscar si el texto del elemento incluye "ProAr"
  if (elemento.innerHTML.includes("ProAr")) {
    // Aplicarle estilo a la instancia encontrada
    elemento.innerHTML = elemento.innerHTML.replace(/ProAr/g, '<span style="font-style: italic; font-weight: bold; color: black;">ProAr</span>');
  }
};*/

